const { MessageEmbed } = require('discord.js')
const db = require('quick.db')

module.exports = {
  name: 'daily',
  description: 'Get your daily coins.',
  aliases: ['dailycoins', 'dailyreward'],
  category: 'economy',
  run: async (client, message, args) => {
    
    let cooldown = 8.64e+7,
        amount = Math.floor((Math))
    
    
  }
}